#include "game/moves.h"
#include "utils.h"
#include <cstdint>
#include <vector>

std::vector<uint64_t> Pawn::makeMoves(uint64_t state, bool colour) {
  std::vector<uint64_t> moves;
  return moves;
};

std::vector<uint64_t> King::getPseudoLegalMoves(uint64_t state, bool colour) {
  std::vector<uint64_t> moves;
  return moves;
};
