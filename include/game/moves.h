#include <cstdint>
#include <vector>

class Pawn {
public:
  std::vector<uint64_t> makeMoves(uint64_t state, bool colour) {
    std::vector<uint64_t> moves;

    if (colour) {
    }

    return moves;
  }
};
